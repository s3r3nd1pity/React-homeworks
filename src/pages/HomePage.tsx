import HomePageComponent from "../components/home-page/HomePageComponent.tsx";

const HomePage = () => {
    return (
        <div>
            <HomePageComponent/>
        </div>
    );
};

export default HomePage;